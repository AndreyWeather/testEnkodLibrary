package com.example.testenkodlibrary

import android.content.Context
import android.widget.Toast

class runClass {
    fun go (context: Context) {
        Toast.makeText(context, "go", Toast.LENGTH_SHORT).show()
    }
}